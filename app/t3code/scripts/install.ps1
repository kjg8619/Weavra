[Console]::Error.WriteLine('Weavra server installation is unavailable: no independent hosted release source is configured.')
[Console]::Error.WriteLine('Build from the Weavra source checkout using its documented setup. The terminal CLI is weavra; the separate app server executable is weavra-server.')
exit 1
