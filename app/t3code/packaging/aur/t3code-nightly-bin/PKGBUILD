# Historical packaging path retained; it is not a Weavra distribution source.
printf '%s\n' 'Weavra AUR installation is unavailable: no independent release artifact source is configured.' >&2
return 1 2>/dev/null || exit 1
